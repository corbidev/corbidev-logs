name: Tests

on:
  pull_request:
    branches:
      - develop

  pull_request:

jobs:
  tests:
    uses: LeZouzouEnWeb/workflows/.github/workflows/symfony.yml@v1.0.11

    with:
      symfony-dir: log
